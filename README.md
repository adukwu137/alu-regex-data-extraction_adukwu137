# ALU Regex Data Extraction & Secure Validation System

This project is a Python-based regular expression data extraction and defensive validation tool designed to parse unstructured, production-style text logs and securely isolate sensitive and structured records.

## Project Structure

```text
alu-regex-data-extraction_{GithubUsername}/
├── input/
│   └── raw-text.txt
├── src/
│   └── main.py
├── output/
│   └── sample-output.json
└── README.md